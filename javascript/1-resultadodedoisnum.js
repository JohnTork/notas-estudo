//1. Peça ao usuário dois números com casas decimais (números reais), some os dois e mostre o resultado.

const numero1 = Number(prompt('Digite um Número: '))
const numero2 = Number(prompt('Digite um Número: '))

const soma = numero1 + numero2

alert("A soma entre os números: " + numero1 + " e " + numero2 + " é igual a " + soma)